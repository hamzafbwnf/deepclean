[/Script/Engine.GameUserSettings]
; Start uncapped and without VSync so performance is not silently limited to the display refresh rate.
bUseVSync=False
FrameRateLimit=0.000000

[ScalabilityGroups]
; Medium GI keeps Lumen enabled but switches to the substantially cheaper Irradiance Field Gather path.
; The packaged stat GPU comparison reduced the Lumen compute queue from about 4.98 ms to 1.95 ms.
sg.GlobalIlluminationQuality=1
